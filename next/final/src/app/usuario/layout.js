import Link from 'next/link'

export const metadata = {
    title: "comerciante",
  }

export default function comercianteLayout({ children }) {
    return (
      
        <div>
        {children}
        </div>
  
    )
}