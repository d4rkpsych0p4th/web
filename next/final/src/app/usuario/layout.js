import Link from 'next/link'

export const metadata = {
    title: "usuario",
  }

export default function comercianteLayout({ children }) {
    return (
      
        <div>
        {children}
        </div>
  
    )
}